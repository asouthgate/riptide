[package]
name = "riptide"
version = "0.0.2"
edition = "2021"

[dependencies]
image = { version = "0.24", default-features=false, features = ["png", "jpeg"] }
shallow = { path = "../shallow" }
rand = "0.8.4"
crossbeam = "0.8"

[lib]
name = "riptide"
path = "src/lib.rs"
